language: python
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
  - "pypy"
# command to install dependencies
install:
  - pip install .
  - pip install -r userservice/requirements.txt
  - pip install -r todoservice/requirements.txt

# command to run tests
script: nosetests
